GUIDE.md
*.log

# Vendored offline assets - produced by `npm run setup`, never committed
vendor/

# Node / test tooling (only needed on the machine that prepares the workshop)
node_modules/
test-results/
playwright-report/
